import 'dotenv/config';

const main = async () => {
  console.log('[fab-card-registry] sync starting…');
  // TODO: drive the @codex/core import use-cases (classes, talents, cards, …) against the database.
  console.log('[fab-card-registry] nothing to sync yet.');
};

void main();
